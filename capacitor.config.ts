import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.vrajera',
  appName: 'VrajeraApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
